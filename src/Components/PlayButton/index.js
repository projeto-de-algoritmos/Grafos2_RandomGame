import React from 'react';

const PlayButton = ({
    title, addItem, removeItem
}) => {
    
     return (
       <div>
        <button>Começar</button>
       </div>
     );
   };
   
   export default PlayButton;